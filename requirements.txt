# Biotech Screener Dependencies
# Install with: pip install -r requirements.txt

# Testing
pytest>=7.0.0

# Note: The core codebase uses only Python stdlib (csv, json, hashlib, dataclasses, etc.)
# Add dependencies below as needed:

# Data processing (uncomment when needed)
# pandas>=2.0.0
# numpy>=1.24.0

# API access (uncomment when needed)
# requests>=2.28.0
# yfinance>=0.2.0

# Database (uncomment when needed)
# psycopg2-binary>=2.9.0  # PostgreSQL for AACT direct access
